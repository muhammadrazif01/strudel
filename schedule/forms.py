from django import forms
from .models import Schedule
from django import forms

#class ScheduleForm(forms.Form):
#    day = forms.ChoiceField(choices= Schedule._meta.get_field('day').choices)
#    class Meta:
#        model = Schedule
 #       fields = ['Day']